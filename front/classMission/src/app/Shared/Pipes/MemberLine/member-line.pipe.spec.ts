import { MemberLinePipe } from './member-line.pipe';

describe('MemberLinePipe', () => {
  it('create an instance', () => {
    const pipe = new MemberLinePipe();
    expect(pipe).toBeTruthy();
  });
});
